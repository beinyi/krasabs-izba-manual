import { actButtons, requestButton, TButton } from "@components/Main/buttons";

const requestMap: Array<TButton> = [   //Шаги для выделения и активации
    actButtons[0],
    requestButton[0],
    requestButton[1],
    requestButton[2],
    requestButton[3],
    requestButton[4],
]


export const requestCredits: Array<string> = [             //"Титры" для вывода после прохождения маршрута

]


export default requestMap;